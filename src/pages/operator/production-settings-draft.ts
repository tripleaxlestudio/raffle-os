import type { EventSettings, LocalAsset } from '../../domain/settings/event-settings.types.ts'

export function assetsEqual(left: LocalAsset | undefined, right: LocalAsset | undefined): boolean { return left === undefined || right === undefined ? left === right : left === right || (left.name === right.name && left.type === right.type && left.size === right.size && left.blob === right.blob) }
export function settingsEqual(left: EventSettings, right: EventSettings): boolean { return left.eventId === right.eventId && left.displayName === right.displayName && left.subtitle === right.subtitle && left.primaryColor === right.primaryColor && left.accentColor === right.accentColor && left.safeAreaMargin === right.safeAreaMargin && left.blackoutAppearance === right.blackoutAppearance && left.audioEnabled === right.audioEnabled && left.masterVolume === right.masterVolume && assetsEqual(left.logo, right.logo) && assetsEqual(left.background, right.background) && assetsEqual(left.revealCue, right.revealCue) }
