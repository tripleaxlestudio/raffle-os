import { RoutePlaceholder } from '../../shared/components/RoutePlaceholder.tsx'

export function PendingResultsPage() {
  return (
    <RoutePlaceholder
      title="Pending Results"
      purpose="This page will support reviewing pending raffle results."
    />
  )
}
